# TODO

## Popup: add "unhide controller" button

Add a button to the popup that allows users to unhide the controller after dismissing it via the × button. This provides a non-keyboard recovery path.

Context: upstream removed the × button entirely (commit `30917cf`), but we chose to keep it. This feature would address the discoverability concern upstream had without removing the button.
